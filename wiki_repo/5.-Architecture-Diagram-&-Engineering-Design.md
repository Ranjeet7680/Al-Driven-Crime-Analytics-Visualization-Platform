# 🏗️ Architecture Diagram & Engineering Design

---

## 1. 4-Tier Layered System Architecture

```mermaid
flowchart TD
    subgraph PresentationLayer["1. PRESENTATION & INTERACTION LAYER (Browser Native)"]
        P1["Cinematic Holographic Boot Canvas"]
        P2["Command Center Overview (KPIs + Gauge)"]
        P3["Interactive 2D Geospatial Canvas (Heatmap)"]
        P4["District Intelligence Deep-Dive Modal"]
        P5["Fast Finder Global Command Palette (Ctrl+K)"]
        P6["Native Web Audio Synthesizer (Oscillators)"]
    end

    subgraph LogicLayer["2. INTELLIGENCE & APPLICATION CORE (app.js)"]
        L1["Multi-Horizon AI Forecast Engine (24h/7d/30d)"]
        L2["Digital Twin Tactical Equation Solver"]
        L3["NLP Intent Parser & Speech Hub"]
        L4["Surge Anomaly Detector (+2.5σ Threshold)"]
        L5["Executive Brief Serializer (JSON/Print)"]
    end

    subgraph StateLayer["3. CLIENT STATE & SIGNAL STORE (data.js & localStorage)"]
        S1["202,533 Relational Crime Record Store"]
        S2["37-District Geospatial Coordinate Index"]
        S3["76-Head IPC/SLL Taxonomy Matrix"]
        S4["User Settings & Audio Preference Cache"]
    end

    subgraph InfrastructureLayer["4. CLOUD & DEPLOYMENT LAYER (Zero-Backend)"]
        I1["GitHub Actions CI/CD Pipeline"]
        I2["GitHub Pages High-Availability Edge"]
        I3["Firebase Authentication Wrapper"]
    end

    PresentationLayer <--> LogicLayer
    LogicLayer <--> StateLayer
    LogicLayer <--> InfrastructureLayer
```

---

## 2. Technical Design Principles

1. **Zero Server Dependency (Edge-First):** Complete dataset packaging enables lightning-fast client query execution without network roundtrips.
2. **Deterministic Mathematical Solvers:** Digital Twin simulation and AI forecasting execute via deterministic mathematical models without external API token dependencies.
3. **WCAG AA Compliance:** High-contrast text ratios, tactile sound feedback, screen reader hooks, and keyboard navigability ensure accessibility for all personnel.
